{
  "name": "e2e-browser-tests",
  "version": "1.0.0",
  "description": "Browser E2E tests for Spotify Profile Dump CLI",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
